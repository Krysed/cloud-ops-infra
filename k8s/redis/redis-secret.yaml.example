apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: dev
  labels:
    app: myapp
    component: redis
type: Opaque
data:
  # Base64 encoded values (echo -n 'value' | base64)
  REDIS_PASSWORD: xyz
