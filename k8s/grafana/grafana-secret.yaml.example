apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: dev
  labels:
    app: myapp
    component: grafana
type: Opaque
data:
  # Base64 encoded values (echo -n 'value' | base64)
  GF_SECURITY_ADMIN_PASSWORD: xyz
